package com.example.oria.backend.server

/**
 * Object to represent the route to use to request the server
 */
object HttpRoutes {
    const val HOST = "192.168.1.16:8000/login"
    const val LOGIN = HOST
}